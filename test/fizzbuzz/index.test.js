'use strict'
/* eslint-env jest */

const sut = require('../../lib/fizzbuzz')

describe('fizz buzz game', () => {
  it('should do something', () => {
    const given = ''

    const expected = ''

    const result = sut.someFunction(given)

    expect(result).toEqual(expected)
  })
})
